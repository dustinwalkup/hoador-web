/**
 * Slug of the default network whose communities are offered on the
 * `/community-select` signup step. Mirrors the slug seeded by the
 * multi-community migration / seeds.
 */
export const DEFAULT_NETWORK_SLUG = "kansas-city-metro";
