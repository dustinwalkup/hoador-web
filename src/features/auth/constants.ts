/**
 * Message returned by API when session is missing or expired (401).
 * Used so client can show this message and redirect to login.
 */
export const SESSION_EXPIRED_MESSAGE =
  "Your session expired. Please sign in again.";
