/**
 * E2E Google OAuth mock: code param value. The actual callback is handled by
 * the Better Auth plugin (e2e-google-plugin) so the session cookie format
 * matches getSession().
 */
export const E2E_GOOGLE_CODE = "e2e-test-google";
