# Implementation Plans

Generated by the improve skill on 2026-06-10, against commit `5c32982` (branch
`develop`). **Run 2** (also 2026-06-10, against commit `ad0306e`, after plans
002–008 merged) appended plans 009–015 from an audit of the previously
under-covered domains: service bookings, disputes, messages, reviews,
communities, notifications/push, Meta tracking, and stray API routes. Execute
in the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, run its drift check, honor its STOP conditions,
and update your row when done.

## Execution order & status

| Plan                                                | Title                                                                                                      | Priority | Effort | Depends on                   | Status                                                                                                |
| --------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | -------- | ------ | ---------------------------- | ----------------------------------------------------------------------------------------------------- |
| [001](001-claude-md-and-dx-fixes.md)                | CLAUDE.md, README rewrite, fix self-recursive audit scripts                                                | P2       | S      | —                            | DONE                                                                                                  |
| [002](002-security-hardening.md)                    | Guard DB-wipe script, lock down create-payment-intent, constant-time secrets                               | P1       | S      | —                            | DONE                                                                                                  |
| [003](003-webhook-failure-audit-trail.md)           | Audit-trail for Stripe webhook failures & unmatched payment events                                         | P1       | S      | —                            | DONE                                                                                                  |
| [004](004-deposit-hold-lifecycle-fixes.md)          | Fix release_failed clobber + deposit-hold retry idempotency                                                | P1       | M      | —                            | IN PROGRESS — Steps 1-2 DONE; Step 3 BLOCKED (STOP #4)                                                |
| [005](005-approve-double-charge-guard.md)           | Atomic claim prevents concurrent-approval double charge                                                    | P1       | S      | —                            | IN PROGRESS — Steps 1-2 DONE; Step 3 BLOCKED (STOP #4)                                                |
| [006](006-route-auth-tests.md)                      | Route tests exercising real auth rejection (money/admin/cron)                                              | P2       | M      | 002 (Step 4 only)            | DONE — Steps 1-3 done; Step 4 N/A (route deleted)                                                     |
| [007](007-router-refresh-to-query-invalidation.md)  | Replace router.refresh() with targeted query invalidation                                                  | P3       | M      | —                            | DONE — only service-listing-form was query-backed; rest are RSC (refresh kept)                        |
| [008](008-payment-lifecycle-unification-spike.md)   | Design spike: unify rental/service payment lifecycles (doc only)                                           | P3       | L      | 003, 004, 005                | DONE — verdict GO-LATER (`docs/payment-lifecycle-unification.md`); baselined on 004/005 partial state |
| [009](009-service-accept-charge-safety.md)          | Service-booking accept charge-safety: atomic claim, charge-aware failure handling, deterministic retry key | P1       | M      | — (read 004/005 notes first) | TODO                                                                                                  |
| [010](010-public-test-routes-and-upload-idor.md)    | Gate public dev routes (test-serp/test-upload) + profile-image delete IDOR                                 | P1       | S      | —                            | TODO                                                                                                  |
| [011](011-service-complete-cancel-atomic-guards.md) | Atomic status guards on service complete/cancel (refund+payout double-spend)                               | P1       | M      | 009                          | TODO                                                                                                  |
| [012](012-review-and-lifecycle-test-coverage.md)    | Tests: blind-review system + service payment-lifecycle DAL                                                 | P2       | M      | —                            | TODO                                                                                                  |
| [013](013-search-nplus1-and-detail-waterfall.md)    | Batch searchListings image N+1 + parallelize rental-details fetches                                        | P2       | S      | —                            | TODO                                                                                                  |
| [014](014-notification-convention-hardening.md)     | Notification-convention hardening + releaseExpired guard + arch-doc tier note                              | P3       | S      | —                            | TODO                                                                                                  |
| [015](015-dispute-route-tests.md)                   | Route tests for the five untested dispute endpoints                                                        | P3       | M      | —                            | TODO                                                                                                  |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001 first because its CLAUDE.md lowers the cost/error-rate of every later executor; no hard dependency.
- 006 Step 4 (create-payment-intent tests) is now N/A: the route was deleted 2026-06-10 as dead code (zero callers). The rest of 006 is independent.
- 006 done on branch `advisor/006-route-auth-tests` (unpushed). Drift handled: the approve route already had a `route.test.ts` (from 005) that mocked `@/lib/api/route-helpers` wholesale for Stripe-Connect gating. It was converted to the "mock session, real helpers" pattern (gating coverage preserved, now 403s come from the real `handleApiError`), and a focused `approve/__tests__/route.test.ts` was added for 401/200/paymentFailed mapping. Second admin route chosen for Step 3: `GET /api/admin/networks` (single `communityDAL.listNetworks` dependency). Cron `process-payouts` already had a real-`verifyCronSecret` test; extended with the unset-secret→500 case and a `CronRunHistoryService.recordRun` stub. No production code changed.
- 008 hard-depends on 003/004/005 — the unification design must capture the _fixed_ lifecycle semantics, not the buggy baseline.
- 004 and 005 both touch `src/features/rentals/services/` — execute sequentially (either order), not in parallel worktrees, to avoid conflicts.
- 004 Step 3 (retry idempotency key) is BLOCKED on a plan-design flaw and was NOT implemented. The plan's premise that "cron and retry operate on disjoint statuses" is false: the cron's `findScheduledDepositsNearPickup` query selects both `scheduled` _and_ `failed` rows (`payment-lifecycle.dal.ts:434-443`) and deliberately retries failed holds (`payment-lifecycle-service.ts:318,324`). Giving the renter retry a per-payment-method key while the cron keeps the fixed key would let a concurrent cron+retry on the same card place **two** authorization holds — a new bug, not a fix (STOP condition #4). Steps 1-2 (the higher-severity `release_failed` clobber + expiry-monitor coverage) are done. Step 3 needs a rewrite: either keep cron+retry on a shared payment-method-scoped key (touches the out-of-scope cron path) or exclude `failed` from the cron query so the two truly become disjoint. Plan 008 must baseline on this partial state.
- 005 Step 3 (catch-all claim release) is BLOCKED on its STOP condition #4 and was NOT implemented. The plan's premise for Step 3 — an unexpected throw "between the claim and those handlers (e.g. Stripe SDK network exception outside `tryCatch`)" — doesn't match the method's shape: the charge is fully `tryCatch`-wrapped and both charge-failure handlers reset to `failed` _before_ any throwable call. Every unwrapped throw point inside the proposed wrap region sits **after the charge succeeded** (`rental-service.ts:572` audit log, `:649-653` approval-failed throw, `:656` getRentalByRequestId) and before the approval commit that sets `succeeded` (`rentals.dal.ts:1850`). In that window, catch-reset-to-`failed` makes a _real_ charge re-claimable: a retry takes the `isRetryAfterFailure` path with a fresh `Date.now()` key → second real charge. Today that window actually self-heals: status stays `processing`, a bare re-approve reuses the deterministic key, Stripe replays the same PaymentIntent, approval completes. Step 3 as written would turn that self-heal into a double charge. Steps 1-2 + tests are done on branch `advisor/005-approve-double-charge-guard` (unpushed, unmerged); residual trade-off if merged as-is: a post-charge throw leaves the request stuck in `processing` (no double charge, but unapprovable without manual reset — previously self-healing). Human decision needed: charge-aware catch (reset to `failed` only if no successful charge; on partial success leave `processing` or jump to `succeeded`) and/or the stale-`processing` detector the plan deferred. Plan 008 must baseline on this partial state too.

### Run 2 dependency notes (009–015)

- **009 before 011**: both rewrite `service-booking-service.ts`, and 011's
  cancel guard (`blockWhilePaymentProcessing`) only means something once 009's
  `paymentStatus = "processing"` claim exists. Never run them in parallel
  worktrees.
- **009 is the service-side twin of 004/005** and was designed around their
  BLOCKED findings (a catch that resets to a retryable status after a
  successful charge re-arms a double charge). Its post-charge failure handler
  deliberately leaves the booking claimed rather than retryable — see plan 009
  "Maintenance notes" for the residual trade-off. Plan 008's unification doc
  should re-baseline on 009/011 once they land.
- **012 ↔ 014 ordering is free but coupled**: 012 writes tests asserting the
  reviews service's notification-failure behavior; 014 changes that behavior
  (`console.error` → `captureNonCriticalError`). Whichever lands second
  reconciles the assertions (both plans carry the note).
- 010, 013, 015 are independent of everything.

## Findings considered and rejected (do not re-audit)

- **acceptBooking missing authorization** — false: `service-booking-service.ts:284-289` verifies provider + status server-side.
- **Webhook audit log falsely records failures as processed** — backwards: the log is _skipped_ on failure (corrected version is plan 003).
- **`.toFixed(2)` currency precision loss** — integer cents ÷ 100 is exact at these magnitudes; noise.
- **Notification `.catch()` swallows errors** — they go to Sentry via `captureNonCriticalError`; intentional fire-and-forget on non-critical sends.
- **Missing CSRF protection** — better-auth cookies default SameSite=Lax; no evidence of misconfiguration; modern browsers block the cross-site POST vector.
- **tsconfig.tsbuildinfo committed** — false: untracked (verified via `git ls-files`).
- **Test routes (`/api/test/*`) exposed** — fail-closed (`NODE_ENV === "production"` → 404); residual risk limited to preview deploys that set `E2E_TEST=1`; accepted.
- **Sequential Stripe/notification loops in crons** — batches capped at 20; immaterial at current scale; revisit if batch sizes grow.
- **401 vs 403 distinction on admin routes** — by design; negligible information disclosure.
- **HydrateClient missing on dashboard pages** — stale finding: six high-traffic pages already converted (the `PERFORMANCE_AUDIT_PHASE2.md` doc is what's stale; plan 007 Step 3 marks it).
- **`@faker-js/faker` in prod dependencies** — true but harmless (Next.js bundles only imports; faker is only imported by seed scripts); not worth a plan.
- **God-DAL split (rentals.dal.ts 3,002 lines)** — real, deferred: splitting the highest-churn money DAL without route-level test coverage inverts safe ordering. Revisit after 006 (and 008's verdict).

### Run 2 rejections (audited 2026-06-10 against `ad0306e`)

- **Messages mark-read/unread "race"** — false: conversation participants are immutable (no reassignment vector); the update writes only the caller's own `lastReadAt` column. A non-participant caller produces an empty `set` — harmless.
- **Conversation details IDOR** — false: `messages.dal.ts:262-269` scopes the query by participant (`user1Id = userId OR user2Id = userId`) in the WHERE clause.
- **`releaseExpired` missing `isNull(releasedAt)` guard** — downgraded: the feeding query (`findUnreleasedExpired`) already filters unreleased rows and the SET value is fixed (value-idempotent); only exposure is duplicate notifications on overlapping cron runs. One-line guard folded into plan 014, not a standalone finding.
- **Payout notification failure "marks lifecycle failed"** — misread by the auditor: the catch (`service-payment-lifecycle-service.ts:107-117`) only increments a counter and sends an ops alert; lifecycle stays `completed`. The real (smaller) issue — awaited notification miscounts the run and fires a false alert — is plan 014.
- **`NEXT_PUBLIC_APP_URL` poisoning of the internal PDF fetch** — env vars are trusted deploy config; not a realistic vector.
- **HTTPS enforcement on cron/internal routes** — TLS terminates at the Vercel/GitHub edge; defense-in-depth noise.
- **HOA inquiry PII in ops email** — intentional: ops needs the contact details to follow up; the template already escapes HTML.
- **Profile GET returns the full own-user object** — own data only; data minimization not worth a plan.
- **Meta Pixel / Conversions API** — audited clean: server-side only (`signup`, `accept-legal-documents`, rental approve), PII hashed via `hashMetaField` before sending, no route trusts client-supplied event data.
- **Neon pool `max: 10` tuning** — speculative without load evidence; revisit with `query-tracker` telemetry if saturation appears.
- **Dashboard badge `refetchOnWindowFocus`** — intentional; the hook already consolidated three pollers (6.5× reduction).
- **Upload rate limiting** — real gap, consciously deferred: needs an infra decision (Redis/`@upstash/ratelimit`); a TODO exists in `src/app/api/listings/[listingId]/route.ts`. Revisit when infra is chosen.
- **Dispute evidence cross-community check** — party-to-dispute check is sufficient; strict community isolation is not a stated invariant.
- **Sentry `beforeSend` PII scrubbing** — speculative; current error classes carry no PII.
- **`rentalId`/`requestId` review-lookup asymmetry** — documented in a code comment (`blind-review-service.ts:164-165`) and pinned by plan 012's tests; not separately planned.
- Known pre-identified wrinkle (report, don't fix, in plan 015): `notes` PUT updates the note **before** verifying it belongs to the dispute (`disputes/[id]/notes/route.ts:142-150`) — admin-only surface, one-line reorder when next touched.

## Direction options surfaced for the maintainer (no plans written)

- **Service reviews half-built**: rental blind-reviews shipped; service pages carry `TODO: Re-enable reviews section once service reviews are ready` (`src/app/dashboard/services/listings/[id]/page.tsx:201`, `src/app/dashboard/services/providers/[userId]/page.tsx:141`). Build (M-L, pattern exists) or remove the stubs (S).
- **Community visibility reconcile guard**: known deferred item — moving a community's network strands existing members; should land before further community feature work.
- **Notification preferences** (run 2): every send is unconditional and `notifications.dal.ts` stores history only — no prefs schema. Send sites are centralized, so threading a prefs check is cheap relative to the UX win. Coarse effort: M.
- **Service browse filter parity** (run 2): rentals have a full filter panel; `service-browse-filters.tsx` is minimal although the `serviceListings` schema supports category/community filtering and test TODOs note unimplemented filter badges. Coarse effort: M.
