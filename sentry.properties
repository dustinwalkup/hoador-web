# Sentry configuration for source map uploads
# This file is used by the Sentry CLI to upload source maps during build

defaults.project=javascript-nextjs
defaults.org=hoador

# Auth token should be set via SENTRY_AUTH_TOKEN environment variable
# Do not commit the auth token to version control
